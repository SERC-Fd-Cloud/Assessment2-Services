{
  "name": "frontend-app",
  "version": "1.0.0",
  "main": "app.js",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5",
    "axios": "^1.6.0",
    "dotenv": "^16.0.3",
    "form-data": "^4.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "build": "cd react-src && npm install && npm run build && cp -r build ../build"
  }
}
